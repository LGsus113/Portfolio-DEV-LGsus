---
interface Props {
  id: string;
  className: string;
}

const { id, className } = Astro.props;
---

<section id={id} class={`w-full h-auto px-5 xl:px-0 flex flex-col ${className}`}>
  <slot />
</section>
