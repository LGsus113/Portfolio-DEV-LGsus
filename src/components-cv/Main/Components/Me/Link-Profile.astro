---
interface Props {
  title: string;
  url: string;
}

const { title, url } = Astro.props;
---

<div class="flex items-center gap-2 hover:opacity-100 hover:bg-gray-200">
  <a
    href={url}
    title={title}
    target="_blank"
    rel="noopener noreferrer"
  >
    <slot/>
  </a>
</div>
