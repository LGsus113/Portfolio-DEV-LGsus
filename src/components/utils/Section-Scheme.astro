---
interface Props {
  id: string;
  className: string;
}

const { id, className } = Astro.props;
---

<section id={id} class={`w-full h-auto p-5 xl:p-0 flex flex-col ${className}`}>
  <slot />
</section>
